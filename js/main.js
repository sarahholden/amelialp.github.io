// NOTE: Here it looks like the quotes are smart quotes (curly quotes instead of straight), which aren't usually recognized
// $(‘.sidenav’).hover(function(){
//   $(this).toggleClass(‘linkhover’);}
// );


// NOTE: looks like the navlink was missing a . inside the function
// $('.navlink').on('hover', function(){
//   $('navlink').toggleClass('linkhover');

// });